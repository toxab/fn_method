# Docker Compose environment variables
# Copy this file to .env.docker.local and customize for your environment

# Application
APP_ENV=dev
APP_DEBUG=1

# Ports
NGINX_PORT=8028
MYSQL_PORT=3327
REDIS_PORT=6379
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
ADMINER_PORT=8080
XDEBUG_PORT=9003

# MySQL Configuration
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=fintech_db
MYSQL_USER=fintech_user
MYSQL_PASSWORD=fintech_pass
MYSQL_BUFFER_POOL=256M

# Platform-specific (uncomment for your platform)
# macOS Intel:
# MYSQL_PLATFORM=linux/amd64
# MOUNT_OPTIONS=:cached

# macOS Apple Silicon (M1/M2/M3):
# MYSQL_PLATFORM=linux/arm64
# MOUNT_OPTIONS=:delegated

# Linux:
# MYSQL_PLATFORM=linux/amd64
# MOUNT_OPTIONS=

# Xdebug
XDEBUG_MODE=off
# For development, change to: XDEBUG_MODE=develop,debug

# Redis (optional, for production use strong password)
REDIS_PASSWORD=
